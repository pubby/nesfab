vars @menu
    byte f
    byte g

vars @pee
    byte a
    byte b

vars @poo
    short c
    short d

fn foo(short x, byte y, byte z) byte
    goto mode main()
    return 0

fn bar(short x, short y, short z) byte
    f += z
    byte a = x + y
    z = a + a
    byte b = x + z
    z = b + b
    byte c = y + z
    z = c + c
    return c

mode main() 
    bar(1,2,3)
    f += 2


