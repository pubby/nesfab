ram menuVars
    byte f
    byte g


fn foo(short x, byte y, byte z) byte
    goto mode main()
    return 0

fn bar(byte x, byte y, byte z) byte
    f += z
    byte a = x + y
    z = a + a
    byte b = x + z
    z = b + b
    byte c = y + z
    z = c + c
    return c



mode main()
    foo(0, 1, 2)
    f = 3
    
    

