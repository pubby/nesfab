#ifndef CONVERT_PENGUIN_HPP
#define CONVERT_PENGUIN_HPP

#include <cstddef>

#include "convert.hpp"

conversion_t convert_penguin(char const* const begin, std::size_t size, global_ht penguin_fn);

#endif
